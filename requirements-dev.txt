# Development / test dependencies (mirrors [dependency-groups].dev in pyproject.toml).
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
anyio>=4.6.0
aiosqlite>=0.20.0
ruff>=0.8.0
mypy>=1.13.0
